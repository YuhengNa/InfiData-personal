# InfiData 数据转换交接文档

## 1. 大体背景与介绍

本项目完成两步转换：

```text
源机器人数据
  -> scripts/convert/convert_*.py
InfiData 中间格式
  -> scripts/convert2openpi/convert_infidata_*_to_rlds.py
RLDS格式数据
```



为什么需要分两步转换？ 

- 源数据的格式多种多样，先转换为统一的InfiData格式，便于后续管理
- RLDS格式的数据是直接用于训练的
- 因此，InfiData格式的数据是中间层数据。例如，数据标注等工作，就可以直接在统一的中间层InfiDATA上做
- 而RLDS是导出训练时所需要的。注意，不同的训练可能需要不同的字段，因此，针对最终训练的具体情况，去从中间层数据上跑脚本，导出得到RLDS



### InfiData

InfiData 以 episode 为单位保存。每个 parquet 行对应一帧，包含 state、action、任务文本、
质量信息和视频帧索引。

```text
<infidata-root>/
├── data/<dataset>/chunk-xxx/episode_xxxxxx.parquet
├── meta/
│   ├── episodes.jsonl
│   ├── segments.jsonl
│   ├── tasks.json
│   ├── robots.json
│   └── stats.json
└── videos/
```

### RLDS

RLDS 产物是标准 TFDS 数据集。一个 TFDS example 对应一个 episode：

```text
episode
├── steps: tf.data.Dataset
│   ├── observation.state: float32[state_dim]
│   ├── observation.images: {camera: uint8[H,W,3]}
│   ├── action: float32[action_dim]
│   ├── task / subtask / quality / success / mistake
│   ├── reward / discount
│   └── is_first / is_last / is_terminal
└── episode_metadata
```

磁盘结构为：

```text
<rlds-root>/<variant>/<builder>/<version>/
├── dataset_info.json
├── features.json
└── <builder>-<split>.tfrecord-*
```


| 数据集             | Builder                    | 版本      |
| --------------- | -------------------------- | ------- |
| DROID           | `droid_infidata`           | `1.1.0` |
| RoboCOIN        | `robocoin_infidata`        | `1.0.0` |
| RoboMIND full   | `robomind_full_infidata`   | `1.0.0` |
| AgiBot          | `agibot_infidata`          | `1.0.0` |
| EgoVerse        | `ego_verse_infidata`       | `1.0.0` |
| realworld_piper | `realworld_piper_infidata` | `1.0.0` |

当前仓库已包含上表六类数据的完整转换链路：源数据转 InfiData 的脚本位于
`scripts/convert/`，InfiData 转 RLDS 的脚本位于 `scripts/convert2openpi/`。


不同 state/action 维度、fps 或相机集合会拆成不同 variant。数据会按比例生成 `train`、
`unseen_test` 和 `seen_test`；其中 `seen_test` 是 train 的子集。以版本目录中的
`dataset_info.json` 作为转换成功标志。

## 2. 具体的可复现命令

所有命令从仓库根目录运行。

```bash
cd /mnt/workspace/wudi/InfiData-personal
```

环境安装：

```bash
python -m pip install \
  numpy pandas pyarrow tqdm h5py jsonschema opencv-python \
  scipy zstandard tensorflow tensorflow-datasets av pillow

ffmpeg -version
ffprobe -version
```

下述命令默认全量转换。可通过修改--num_episodes限制最大的转换条数

### 2.1 DROID

```bash
# 源数据 -> InfiData
python scripts/convert/convert_droid.py \
  --droid_root /mnt/workspace/wudi/ELUBrain/DROID \
  --out_root /mnt/workspace/InfiData/DROID \
  --num_episodes 0 \
  --overwrite

# 校验
python scripts/validate/validate_robot_dataset.py \
  /mnt/workspace/InfiData/DROID \
  --report /mnt/workspace/InfiData/DROID/validation_report.json

# InfiData -> RLDS
python scripts/convert2openpi/convert_infidata_droid_to_rlds.py \
  --infidata-root /mnt/workspace/InfiData/DROID \
  --lerobot-root /mnt/workspace/wudi/ELUBrain/DROID \
  --data-dir /mnt/workspace/RLDS/DROID \
  --overwrite
```

DROID RLDS 仍需要原始 DROID 目录，用于从共享视频中准确解码 episode 帧。

### 2.2 RoboCOIN

```bash
# 源数据 -> InfiData
python scripts/convert/convert_robocoin.py \
  --robocoin_root /mnt/workspace/wudi/ELUBrain/RoboCOIN_data/RoboCOIN \
  --out_root /mnt/workspace/InfiData/RoboCOIN \
  --num_episodes 0 \
  --overwrite

python scripts/validate/validate_robot_dataset.py /mnt/workspace/InfiData/RoboCOIN

# InfiData -> RLDS
# 每个 .sh 对应一个 variant；先查看可用脚本
ls scripts/convert2openpi/RoboCOIN/*.sh

# 示例：Airbot MMK2
INFIDATA_ROOT=/mnt/workspace/InfiData/RoboCOIN \
RLDS_ROOT=/mnt/workspace/RLDS/RoboCOIN \
bash scripts/convert2openpi/RoboCOIN/Airbot_MMK2_s36_a36_fps30__episodes_10532.sh
```



### 2.3 RoboMIND

源数据已有四个分片 wrapper：

```bash
# 源数据 -> InfiData
for shard in 0 1 2 3; do
  ROBOMIND_ROOT=/mnt/workspace/wudi/ELUBrain/RoboMIND \
  OUT_ROOT=/mnt/workspace/wudi/InfiData/RoboMIND_shard_${shard} \
  EXTRACT_ROOT=/mnt/workspace/wudi/ELUBrain/RoboMIND_extracted_shards/shard_${shard} \
  OVERWRITE=1 \
  bash scripts/convert/convert_robomind_shard${shard}.sh
done

for shard in 0 1 2 3; do
  python scripts/validate/validate_robot_dataset.py \
    /mnt/workspace/wudi/InfiData/RoboMIND_shard_${shard}
done

# InfiData -> RLDS
# 每个 .sh 对应一个 variant；先查看可用脚本
ls scripts/convert2openpi/RoboMIND_full/*.sh

# 示例：UR5e
INFIDATA_ROOT=/mnt/workspace/wudi/InfiData \
RLDS_ROOT=/mnt/workspace/RLDS/RoboMIND_full \
bash scripts/convert2openpi/RoboMIND_full/ur5e_master_puppet_joint_position_h5_ur_1rgb_real_s7_a7_fps30_cam_top__episodes_26380.sh
```



### 2.4 AgiBotWorld-Beta

```bash
# 源数据 -> InfiData
python scripts/convert/convert_agibot_modelscope.py \
  --agibot_root /mnt/workspace/wudi/ELUBrain/AgiBotWorld-Beta-ModelScope-500h-extracted \
  --metadata_root /mnt/workspace/wudi/ELUBrain/AgiBotWorld-Beta-ModelScope-500h \
  --out_root /mnt/workspace/InfiData/AgiBotWorld-Beta-ModelScope-500h \
  --num_episodes 0 \
  --overwrite

python scripts/validate/validate_robot_dataset.py \
  /mnt/workspace/InfiData/AgiBotWorld-Beta-ModelScope-500h

# InfiData -> RLDS
# 运行现有 AgiBot variant
INFIDATA_ROOT=/mnt/workspace/InfiData/AgiBotWorld-Beta-ModelScope-500h \
RLDS_ROOT=/mnt/workspace/RLDS/AgiBot \
bash scripts/convert2openpi/AgiBot/*.sh
```



### 2.5 realworld_piper

```bash
# 源数据 -> InfiData
python scripts/convert/convert_realworld_piper.py \
  --source_root /mnt/workspace/InfiData/realworld_piper \
  --out_root /mnt/workspace/InfiData/realworld_piper_infidata \
  --num_episodes 0 \
  --overwrite

python scripts/validate/validate_robot_dataset.py \
  /mnt/workspace/InfiData/realworld_piper_infidata


# InfiData -> RLDS
# 查看可用 variant
python scripts/convert2openpi/convert_infidata_realworld_piper_to_rlds.py \
  --infidata-root /mnt/workspace/InfiData/realworld_piper_infidata \
  --list-schemas

# 示例：30 FPS、四相机、包含 EE pose 的 variant
python scripts/convert2openpi/convert_infidata_realworld_piper_to_rlds.py \
  --infidata-root /mnt/workspace/InfiData/realworld_piper_infidata \
  --schema-key piper_s14_a14_fps30_c4_ee_pose \
  --camera-set cam_front,cam_high,cam_left_wrist,cam_right_wrist \
  --data-dir /mnt/workspace/RLDS/realworld_piper/piper_s14_a14_fps30_c4_ee_pose \
  --overwrite
```



### 2.6 EgoVerse

源数据是每个 episode 一个 Zarr v3 目录。单 episode 或小批量复现：

```bash
python scripts/convert/convert_egoverse_zarr_to_infidata.py \
  --input-root /mnt/workspace/wudi/ELUBrain/EgoVerseData \
  --episode-id 2025-09-20-17-42-51-000000 \
  --episode-index 0 \
  --out-root /mnt/workspace/tmp/egoverse_infidata_smoke \
  --clean-output \
  --strict
```

全量数据使用固定 manifest 分为三个 shard，下面的 ranges 会在 manifest 末尾自动截断：

```bash
python scripts/convert/run_egoverse_infidata_shards.py \
  --input-root /mnt/workspace/wudi/ELUBrain/EgoVerseData \
  --output-base /mnt/workspace/InfiData \
  --prefix EgoVerse_full \
  --manifest /mnt/workspace/InfiData/EgoVerse_full_manifest.jsonl \
  --ranges 0:20000,20000:40000,40000:70000 \
  --clean-output

for shard in 1 2 3; do
  python scripts/validate/validate_robot_dataset.py \
    /mnt/workspace/InfiData/EgoVerse_full_${shard}
done
```

转换后的笛卡尔字段为：

```text
observation.state:  [12]      = 当前 [left xyz ypr, right xyz ypr]
actions_cartesian: [100, 12] = 未来约 30 个源帧，插值为 100 点
action:             [12]      = actions_cartesian[0]
```

`obs_head_pose` 存在时，整段轨迹转到当前头部坐标系；否则保持 source pose frame。
`actions_cartesian` 来自未来的 `left/right.obs_ee_pose`，是观测到的双手末端轨迹，
不是速度、力矩或 EVA `cmd_ee_pose`。

每个 wrapper 对应一个 variant：

```bash
# InfiData -> RLDS
ls scripts/convert2openpi/EgoVerse/*.sh
ls scripts/convert2openpi/EgoVerse_full/*.sh

# 示例：单目录 aria_bimanual
INFIDATA_ROOT=/mnt/workspace/InfiData/EgoVerse \
RLDS_ROOT=/mnt/workspace/RLDS/EgoVerse \
bash scripts/convert2openpi/EgoVerse/aria_bimanual__episodes_625.sh

# 示例：从三个 full shard 只生成 aria_bimanual 这一个 variant
bash scripts/convert2openpi/EgoVerse_full/aria_bimanual_front_1__episodes_1031.sh
```



## 3. 转换新数据集应该怎么跑



### 3.1 源数据转 InfiData

参考结构最接近的 `scripts/convert/convert_*.py`：

1. 读取源 episode、state、action、任务和视频。
2. 每个 episode 写一个 parquet，`frame_index` 从 0 连续递增。
3. 写出 `episodes.jsonl`、`segments.jsonl`、`tasks.json`、`robots.json` 和 `stats.json`。
4. 视频字段统一为 `video.<camera>.path/frame_index`。
5. 先转换 1 至 3 个 episode 并运行校验器。

```bash
python scripts/convert/convert_<dataset>.py \
  --source_root /path/to/raw-data \
  --out_root /path/to/InfiData/<dataset> \
  --num_episodes 3 \
  --strict

python scripts/validate/validate_robot_dataset.py \
  /path/to/InfiData/<dataset>
```



### 3.2 InfiData 转 RLDS

参考结构最接近的 `convert_infidata_*_to_rlds.py`，实现 TFDS
`GeneratorBasedBuilder`：从 `episodes.jsonl` 枚举 episode，从 parquet 读取逐帧数据，
解码视频到 `observation.images`，并按固定 state/action/image shape 拆分 variant。

```bash
python scripts/convert2openpi/convert_infidata_<dataset>_to_rlds.py \
  --infidata-root /path/to/InfiData/<dataset> \
  --data-dir /path/to/RLDS/<dataset> \
  --max-episodes 1 \
  --unseen-test-ratio 0 \
  --seen-test-ratio 0 \
  --overwrite
```

小样本通过后去掉 `--max-episodes 1` 运行全量。完成后检查 `dataset_info.json` 和
`skipped_episodes.jsonl`。
